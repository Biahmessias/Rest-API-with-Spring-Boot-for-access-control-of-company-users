version: "3"
services:
  postgres:
    container_name: bancoHoras
    image: postgres:latest
    environment:
      POSTGRES_USER: banco
      POSTGRES_PASSWORD: swadm
      POSTGRES_DB: bancoHoras
    ports:
      - "5432:5432"